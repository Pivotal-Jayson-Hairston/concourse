set -e

tar -xzf fly_binaries/fly_*.tgz ./

for platform in linux darwin windows; do
  mkdir -p ${BOSH_INSTALL_TARGET}/${platform}/amd64
  mv ./fly_${platform}_amd64 ${BOSH_INSTALL_TARGET}/${platform}/amd64/fly
done
