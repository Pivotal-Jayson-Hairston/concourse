---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/atc-ci

inputs:
  - name: version
  - name: concourse
  - name: linux
    path: binaries
  - name: darwin
    path: binaries
  - name: windows
    path: binaries

outputs:
  - name: flies

params:
  platform: linux

run:
  path: concourse/ci/scripts/tarball-flies
